package pt.unl.fct.di.hyflexchain.util;

/**
 * An interface that defines a single method -
 * {@link #reset()}. Any class that implements this interface
 * must be able to perform a reset.
 */
public interface ResetInterface {
    
    /**
     * Perform a reset on this component.
     */
    void reset();

}
